let pi = 3.14
let raioCirculo = 1
let comprimentoCirculo 
let areaCirculo  
areaCirculo = (Math.PI * Math.pow(raioCirculo, 2));
comprimentoCirculo  = 2 * pi;
   
(console.log(comprimentoCirculo, areaCirculo).toFixed(2))